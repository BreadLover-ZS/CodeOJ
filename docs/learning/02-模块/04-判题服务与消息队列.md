# 02 模块 · 判题服务与消息队列

> 阅读范围：判题服务的 RabbitMQ、JudgeService 和策略代码，以及题目服务的消息生产者。先读[网关与题目服务](03-网关与题目服务.md)。

## Part A：RabbitMQ —— 判题任务的"快递线"

### A1. 拓扑 [InitRabbitMqBean.java](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/rabbitmq/InitRabbitMqBean.java)

判题服务启动时（`@PostConstruct`）声明：

```text
direct exchange: code_exchange
        |
        | routing key: my_routingKey
        v
durable queue:  code_queue
```

- `queueDeclare(code_queue, true, ...)`：持久化的是队列定义；这不能单独证明每条业务消息都已可靠发布、路由并消费。
- 生产端在题目服务，消费端在判题服务，中间通过 exchange + routingKey + queue 绑定打通。

### A2. 消费者 [MyMessageConsumer.java](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/rabbitmq/MyMessageConsumer.java)

按源码逐行看这段消费逻辑：

| 时点 | 当前动作 | 需要留意的边界 |
| --- | --- | --- |
| 进入 `try` 前 | `Long.parseLong(message)` | 解析失败不进入该方法的 catch |
| `doJudge` 成功 | `basicAck(deliveryTag, false)` | 结果写回与 ACK 分开，期间宕机会重投 |
| `doJudge` 抛异常 | 再查提交记录；非终态时尝试写 FAILED | 查状态本身失败会中断后续 NACK；写 FAILED 失败只记日志 |
| catch 末尾 | `basicNack(deliveryTag, false, false)` | `requeue=false`；当前队列未见 DLX 配置 |

三个关键理解点：

1. **MANUAL + basicAck/basicNack**：手动确认。只有业务处理成功才 ack；异常时 nack 且 **requeue=false**（不重回队列，防止坏消息反复消费）。
2. **异常时的状态处理**：catch 里再次查提交；若不是 SUCCEED/FAILED，尝试更新为 FAILED，然后 `basicNack(requeue=false)`。回写失败只记日志，之后仍 NACK，因此可能留下未恢复的 WAITING/RUNNING。读取当前状态本身失败时，代码也未进入后面的 NACK。
3. **终态重复消息**：`doJudge` 看到非 WAITING 会抛异常，消费者 catch 再查终态并避免覆盖；这是一条缓解路径，不能叫完整幂等协议。当前未见有界重试、DLQ 或自动恢复闭环。

## Part B：判题编排（JudgeServiceImpl · 读三遍）

### B1. 主流程 [JudgeServiceImpl#doJudge](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/judge/JudgeServiceImpl.java)

下面按源码顺序摘出关键语义，省略异常处理和部分字段，不可直接当作可编译实现：

```java
@Override
public QuestionSubmit doJudge(long questionSubmitId) {
    // 1) 取提交记录（Feign 调题目服务）
    QuestionSubmit questionSubmit = questionFeignClient.getQuestionSubmitById(questionSubmitId);
    if (questionSubmit == null) throw new BusinessException(NOT_FOUND_ERROR, "提交信息不存在");

    // 2) 取题目（含测试用例 judgeCase、judgeConfig）
    Question question = questionFeignClient.getQuestionById(questionSubmit.getQuestionId());
    if (question == null) throw new BusinessException(NOT_FOUND_ERROR, "题目不存在");

    // 3) 先读状态；这里只是普通检查，不是原子抢占
    if (!status.equals(WAITING)) throw new BusinessException(OPERATION_ERROR, "题目正在判题中");

    // 4) 普通更新为 RUNNING；并发时两个 worker 仍可能都通过上一步
    QuestionSubmit questionSubmitUpdate = new QuestionSubmit();
    questionSubmitUpdate.setId(questionSubmitId);
    questionSubmitUpdate.setStatus(RUNNING);
    boolean update = questionFeignClient.updateQuestionSubmitById(questionSubmitUpdate);
    if (!update) throw new BusinessException(SYSTEM_ERROR, "题目状态更新错误");

    // 5) 通过工厂创建沙箱（remote/example/thirdParty），并用代理包装
    CodeSandbox codeSandbox = codeSandboxFactory.newInstance(type);   // type = codesandbox.type
    codeSandbox = new CodeSandboxProxy(codeSandbox);

    // 6) 组装 ExecuteCodeRequest：code + language + inputList（用例的输入）
    List<JudgeCase> judgeCaseList = JSONUtil.toList(question.getJudgeCase(), JudgeCase.class);
    List<String> inputList = judgeCaseList.stream().map(JudgeCase::getInput).collect(Collectors.toList());
    ExecuteCodeResponse response = codeSandbox.executeCode(executeCodeRequest);

    // 7) 组装 JudgeContext，交给 JudgeManager（策略模式）判题
    JudgeInfo judgeInfo = judgeManager.doJudge(judgeContext);

    // 8) 回写 status=SUCCEED + judgeInfo(JSON)，存库
    // 9) 若 Accepted，累加题目通过数（updateQuestionStats，失败仅 warning）
    // 10) 返回最新提交记录
}
```

### B2. 三层状态与竞态

- `QuestionSubmit.status = SUCCEED`：判题流程走到写回结果，不代表答案正确。
- `ExecuteCodeResponse.status`：沙箱这次调用成功或失败；当前编译/运行失败时，沙箱返回 `status=2`，但 `judgeInfo` 为空。
- `JudgeInfo.message`：用户代码的 verdict，例如 Accepted、Wrong Answer。判题服务当前没有先对沙箱失败状态分支，策略读取空 `judgeInfo` 可能抛异常，最终由消费者写 FAILED；不要声称 CE/RE/TLE 已稳定落成 verdict。

状态检查与 `updateQuestionSubmitById` 分离。两个消费者并发读取同一 WAITING 时，均可能把它改成 RUNNING 并执行。基于 `WHERE status=WAITING` 的条件更新是待实现方案；即使加上它，RUNNING 后 worker 崩溃还需要单独的恢复设计。

### B3. Feign 调用的边界

判题服务本身**不连数据库**，一切数据通过 `QuestionFeignClient`（题目服务）读写：查题目、查提交、改提交、累加统计。这是"按业务拆分服务"的体现——判题服务只关心"判"，存取都委托题目服务。

## Part C：判题策略（策略模式）

### C1. 入口 [JudgeManager.java](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/judge/JudgeManager.java)

```java
JudgeInfo doJudge(JudgeContext judgeContext) {
    JudgeStrategy judgeStrategy = new DefaultJudgeStrategy();
    if ("java".equals(language)) {
        judgeStrategy = new JavaLanguageJudgeStrategy();   // 按语言选择策略
    }
    return judgeStrategy.doJudge(judgeContext);
}
```

### C2. Java 策略 [JavaLanguageJudgeStrategy.java](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/judge/strategy/JavaLanguageJudgeStrategy.java)

判题顺序（代码逐行对应）：

1. 取 judgeInfo 的 time / memory，若无则为 0。
2. 输出数量 != 输入数量 → `WRONG_ANSWER`（用例都没跑齐）。
3. 逐个用例比对 `judgeCase.output` 与沙箱实际输出 → 不等 → `WRONG_ANSWER`。
4. `memory > memoryLimit` → `MEMORY_LIMIT_EXCEEDED`。
5. `time - 10000L > timeLimit` → `TIME_LIMIT_EXCEEDED`（注释："Java 程序本身需要额外执行 10 秒"——这条规则从原版项目继承，含义是给 Java 虚拟机启动留缓冲；本沙箱记录的是进程实际耗时，若要更准确需要测试校准，审计向文档已提示）。
6. 全部通过 → `ACCEPTED`。

### C3. 默认策略 [DefaultJudgeStrategy.java](../../../codeoj-backend/codeoj-backend-judge-service/src/main/java/com/codeoj/codeojbackendjudgeservice/judge/strategy/DefaultJudgeStrategy.java)

与 Java 策略几乎一致，只是**没有 Java 的 10 秒缓冲**。

### C4. 策略模式收益

新增一种语言（如 go/cpp 已有枚举）：

- 实现一个 `XxxLanguageJudgeStrategy implements JudgeStrategy`；
- 在 `JudgeManager` 里加一个分支；
- 若沙箱语言不同，再扩展 `ExecuteCodeController` 对应执行器。
- 不会动到 `doJudge` 主流程——这就是把"判什么"和"怎么判"解耦。

## 关键问题

1. 消费成功和异常时分别在什么时机 ACK/NACK？`requeue=false` 后消息会怎样？
2. 两个消费者为何可能同时执行同一提交？当前的 WAITING 检查缺少什么？
3. 沙箱返回 `status=2` 且 `judgeInfo=null` 时，当前判题服务接下来会做什么？
4. `SUCCEED`、沙箱 `status=1`、`Accepted` 各代表哪一层结果？
